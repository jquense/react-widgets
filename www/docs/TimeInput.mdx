---
sidebar_label: TimeInput
---

import KeyboardShortcuts from "@site/src/components/KeyboardShortcuts";
import TimeInputMetadata from "@react-metadata/TimeInput";
import PropsList from "@theme/PropsList";

## Overview

:::warning Experimental
TimeInput is an Experimental component, it's API may change, and even break across
minor versions (though we will do our best to avoid that).
:::

```jsx live showImports
import TimeInput from "react-widgets/TimeInput";

<TimeInput style={{ width: "auto" }} />;
```

<PropsList props={TimeInputMetadata.props} />
